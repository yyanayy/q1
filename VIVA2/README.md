# Guide for Git Command
## Sample Command
use this command to get status of repository 

```bash
$ git status
$ git add <filename>
$ git commit -m '<Message>'
$ git pull origin
$ git push origin 
```
continue...